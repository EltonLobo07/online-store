import styled from "styled-components";

const InputContainerFlex = styled.div`
    display: flex;
    align-items: center;

    padding-right: ${props => props.paddingRight};
`;

export default InputContainerFlex;
